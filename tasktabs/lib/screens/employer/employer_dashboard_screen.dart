
import 'package:flutter/material.dart';

class EmployerDashboardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Employer Dashboard"),
      ),
      body: Center(
        child: Text("Employer Dashboard Screen"),
      ),
    );
  }
}

