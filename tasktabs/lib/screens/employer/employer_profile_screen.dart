
import 'package:flutter/material.dart';

class EmployerProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Employer Profile"),
      ),
      body: Center(
        child: Text("Employer Profile Screen"),
      ),
    );
  }
}

