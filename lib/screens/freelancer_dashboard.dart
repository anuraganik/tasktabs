import 'package:flutter/material.dart';

class FreelancerDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Freelancer Dashboard'),
      ),
      body: Center(
        child: Text('Freelancer Dashboard Here'),
      ),
    );
  }
}

